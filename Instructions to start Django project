New Django project

python3 -m venv env1 | makes a virtual environment with name env1 and venv is the python module that makes it.
-----------
To activate the environment in linux:

source env1/bin/activate
------------------------

In the same terminal:

pip install django | to install django.

----------------

python3 manage.py runserver | to start django webserver (django has its own webserver)



